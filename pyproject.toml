[project]
name = "sppr"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sam", email = "samdelhaye06@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
]

[project.scripts]
sppr = "sppr:main"

[build-system]
requires = ["uv_build>=0.12.14,<0.13.0"]
build-backend = "uv_build"
